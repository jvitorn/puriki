node_modules/
.expo/
coverage/
dist/
/android/
/ios/
modules/**/android/build/
modules/**/android/.gradle/
.DS_Store
*.log

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

# expo-env.d.ts is committed on purpose: it only declares "expo/types" (no
# machine-specific content) and CI's `npm run typecheck` needs it, but it is
# only ever regenerated by `expo start`, which CI never runs.
.env
.env.local
.env.*.local
!.env.example
# @end expo-cli
